{
  "name": "looplock-extension",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build:content -- --watch & npm run build:app -- --watch",
    "build:content": "vite build --mode content",
    "build:app": "vite build --mode app",
    "build": "npm run build:content && npm run build:app"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.292",
    "@types/node": "^25.5.2",
    "typescript": "^5.9.3",
    "vite": "^5.4.21"
  }
}